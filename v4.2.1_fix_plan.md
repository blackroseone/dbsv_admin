# v4.2.1 修复方案（4 项）

> 本文档是给执行者（dsv4flash）的精确修改说明。每项均给出文件、位置、修改前/后代码。
> 改完统一验证：`python -m py_compile routes/agent.py agent/engine.py` + `node --check static/js/agent.js`

---

## ⚠️ 已完成的修改（不要重复改）

以下 3 处本次已经改完，执行者跳过：

1. `static/css/style.css` `.agent-sidebar`：`width: 260px` → `width: 320px`（已加注释）
2. `static/css/style.css` `.drawer-left`：`width: 260px` → `width: 320px`（已加注释，与侧栏同宽）
3. `static/js/agent.js` `renderAgentScopeTree()` 内：实例名、服务器名的 `<span class="scope-node-name">` 已加 `title` 悬浮提示（2 处）

---

## 修复 1（剩余部分）：池名加悬浮提示

**文件**：`static/js/agent.js` `renderAgentScopeTree()` 内（池渲染模板，搜 `scope-pool-name`）

```js
// 修改前
<span class="scope-pool-name">${escapeHtml(pool.name)}</span>

// 修改后
<span class="scope-pool-name" title="${escapeHtml(pool.name)}">${escapeHtml(pool.name)}</span>
```

---

## 修复 2：会话级"历史记忆"开关（默认开，可按会话关闭）

背景：每次 `/api/agent/run` 都新建 `SmartOpsAgent`，`conversation_history` 从空开始，**会话间本就不共享对话历史**。用户感知到的"旧会话上下文"来自长期记忆召回（`_recall_memory` 把历史诊断沉淀的 `agent_memory` 注入 system prompt"环境上下文（历史记录，供参考）"）。本修复加会话级开关，不做全局关闭。

### 2.1 后端：`agent/engine.py`

**(a) `SmartOpsAgent.__init__` 签名加参数**（约 line 61-64）：

```python
# 修改前
def __init__(self, session_id: str, ssh_conn_id: Optional[str] = None,
             db_conn_id: Optional[str] = None, model_id: Optional[str] = None,
             scope: Optional[List[Dict]] = None,
             manual_skill_name: Optional[str] = None):

# 修改后
def __init__(self, session_id: str, ssh_conn_id: Optional[str] = None,
             db_conn_id: Optional[str] = None, model_id: Optional[str] = None,
             scope: Optional[List[Dict]] = None,
             manual_skill_name: Optional[str] = None,
             disable_memory: bool = False):
```

**(b) `__init__` 体内保存**（建议加在 `self.manual_skill = ...` 附近）：

```python
        # v4.2.1 会话级开关：关闭后不召回长期记忆（跨会话环境上下文）
        self.disable_memory = disable_memory
```

**(c) `_react_loop` 记忆召回处**（约 line 168-169，搜 `# 2.5 长期记忆召回`）：

```python
# 修改前
        # 2.5 长期记忆召回（环境上下文）
        memory_refs = self._recall_memory(user_question)

# 修改后
        # 2.5 长期记忆召回（环境上下文）；会话级开关关闭时跳过（v4.2.1）
        memory_refs = [] if self.disable_memory else self._recall_memory(user_question)
```

### 2.2 后端：`routes/agent.py` `run_agent()`

**(a) 参数读取**（约 line 175，`skill_name = data.get('skill_name')` 之后）：

```python
    skill_name = data.get('skill_name')  # 可选：手动指定技能（v4.0）
    disable_memory = bool(data.get('disable_memory', False))  # v4.2.1 会话级关闭长期记忆召回
```

**(b) 构造 SmartOpsAgent 时传入**（`generate()` 内，约 line 209）：

```python
        agent = SmartOpsAgent(
            session_id=session_id,
            ssh_conn_id=ssh_conn_id,
            db_conn_id=db_conn_id,
            scope=scope,
            manual_skill_name=skill_name,
            model_id=model_id,
            disable_memory=disable_memory
        )
```

### 2.3 前端：`static/js/agent.js`

**(a) `createAgentPane(sid)` 输入区加开关**（搜 `input-hints`，该函数内唯一一处）：

```js
// 修改前
            <div class="input-hints">
                <span>💡 / 调用技能与指令 · 试试：对所选节点批量查询 max_connections</span>
            </div>

// 修改后
            <div class="input-hints">
                <label class="checkbox-label" title="开启后注入历史诊断沉淀的长期记忆（环境上下文）；关闭则该会话完全独立">
                    <input type="checkbox" id="agent-use-memory-${sid}" checked> 历史记忆
                </label>
                <span>💡 / 调用技能与指令 · 试试：对所选节点批量查询 max_connections</span>
            </div>
```

注：`checkbox-label` 样式已存在（QA 模块在用）。若 `.input-hints` 布局异常，在 style.css 里给 `.input-hints .checkbox-label` 加 `margin: 0; font-size: 0.75rem; color: var(--text-secondary); white-space: nowrap;`。

**(b) `sendAgentQuestion(sid)` 发送时带上开关**：

```js
// 修改前
        const response = await fetch('/api/agent/run', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                session_id: sid,
                question,
                skill_name: view.selectedSkill || null   // v4.0 手动技能
            }),
            signal: view.controller.signal
        });

// 修改后
        // v4.2.1 会话级历史记忆开关（默认开）
        const memEl = document.getElementById(`agent-use-memory-${sid}`);
        const useMemory = memEl ? memEl.checked : true;
        const response = await fetch('/api/agent/run', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                session_id: sid,
                question,
                skill_name: view.selectedSkill || null,   // v4.0 手动技能
                disable_memory: !useMemory                // v4.2.1 关闭长期记忆召回
            }),
            signal: view.controller.signal
        });
```

---

## 修复 3：补配连接后范围栏状态不刷新（bug）

**原因**：`saveSSHConnection()` / `saveDBConnection()` 成功后只刷新连接列表，没调 `refreshAgentScopeResolve()`，徽标停留在旧解析结果。

**文件**：`static/js/agent.js`

**(a) `saveSSHConnection()` 成功分支**（约 line 2315）：

```js
// 修改前
        if (response.ok) {
            showToast('SSH连接添加成功', 'success');
            closeModal('modal-add-ssh');
            loadAgentSSHConnections();
        } else {

// 修改后
        if (response.ok) {
            showToast('SSH连接添加成功', 'success');
            closeModal('modal-add-ssh');
            loadAgentSSHConnections();
            refreshAgentScopeResolve();   // v4.2.1 立即重解析范围徽标（修复"仍显示未配置"）
        } else {
```

**(b) `saveDBConnection()` 成功分支**（约 line 2354，同样处理）：

```js
// 修改前
        if (response.ok) {
            showToast('数据库连接添加成功', 'success');
            closeModal('modal-add-db');
            loadAgentDBConnections();
        } else {

// 修改后
        if (response.ok) {
            showToast('数据库连接添加成功', 'success');
            closeModal('modal-add-db');
            loadAgentDBConnections();
            refreshAgentScopeResolve();   // v4.2.1 立即重解析范围徽标
        } else {
```

---

## 修复 4：移除连接卡片"选中"行为，范围只在左侧选

**背景**：管理抽屉里点击连接卡片会设置全局 `agentCurrentSSHConn`/`agentCurrentDBConn`，当左侧范围面板无勾选时，新会话会退化为"legacy 单连接会话"并静默以该连接为执行目标；且加载时会自动选中第一个连接。按新理念：**操作范围唯一入口 = 左侧范围面板勾选**。

### 4.1 `static/js/agent.js` 共 8 处

**(1) 删除顶部全局变量**（约 line 10-11）：

```js
// 删除这两行
let agentCurrentSSHConn = null;
let agentCurrentDBConn = null;
```

**(2) `loadAgentSSHConnections()`：删除自动选中块**：

```js
// 删除这 3 行（保留 loadAgentSSHConnections 其余部分）
        // 未选中时自动选中第一个，避免配置后还需手动点选才能开会话
        if (!agentCurrentSSHConn && agentSSHConnections.length > 0) {
            agentCurrentSSHConn = agentSSHConnections[0].id;
        }
```

**(3) `loadAgentDBConnections()`：同样删除自动选中块**：

```js
// 删除这 3 行
        // 未选中时自动选中第一个
        if (!agentCurrentDBConn && agentDBConnections.length > 0) {
            agentCurrentDBConn = agentDBConnections[0].id;
        }
```

**(4) `renderAgentSSHConnections()`：卡片改为纯展示**：

```js
// 修改前
        <div class="connection-item ${agentCurrentSSHConn === conn.id ? 'active' : ''}"
             onclick="selectSSHConnection('${escapeJsAttr(conn.id)}')">

// 修改后（v4.2.1 管理抽屉纯管理，卡片不再选中；范围只在左侧面板勾选）
        <div class="connection-item">
```

**(5) `renderAgentDBConnections()`：同样处理**：

```js
// 修改前
        <div class="connection-item ${agentCurrentDBConn === conn.id ? 'active' : ''}"
             onclick="selectDBConnection('${escapeJsAttr(conn.id)}')">

// 修改后
        <div class="connection-item">
```

**(6) 整体删除 `selectSSHConnection` 和 `selectDBConnection` 两个函数**（约 line 649-659）。

**(7) `updateAgentConnectionStatus()` 改为从当前会话读连接**（legacy 会话状态展示仍可用）：

```js
// 修改前
function updateAgentConnectionStatus() {
    const sshStatus = document.getElementById('agent-ssh-status');
    const dbStatus = document.getElementById('agent-db-status');

    if (sshStatus) {
        const sshConn = agentSSHConnections.find(c => c.id === agentCurrentSSHConn);
        sshStatus.textContent = sshConn ? `🟢 ${sshConn.name}` : '🔴 SSH未连接';
    }

    if (dbStatus) {
        const dbConn = agentDBConnections.find(c => c.id === agentCurrentDBConn);
        dbStatus.textContent = dbConn ? `🟢 ${dbConn.name}` : '🔴 DB未连接';
    }
}

// 修改后（v4.2.1 从当前会话的连接字段推导，不再依赖全局选中）
function updateAgentConnectionStatus() {
    const sshStatus = document.getElementById('agent-ssh-status');
    const dbStatus = document.getElementById('agent-db-status');
    const s = agentSessions.find(x => x.id === agentCurrentSession);

    if (sshStatus) {
        const sshConn = agentSSHConnections.find(c => c.id === (s && s.ssh_connection_id));
        sshStatus.textContent = sshConn ? `🟢 ${sshConn.name}` : '🔴 SSH未连接';
    }

    if (dbStatus) {
        const dbConn = agentDBConnections.find(c => c.id === (s && s.db_connection_id));
        dbStatus.textContent = dbConn ? `🟢 ${dbConn.name}` : '🔴 DB未连接';
    }
}
```

**(8) `updateAgentScopeBadge(sid)` 开头补一行调用**（切页签时同步刷新 legacy 状态文案）：

```js
// 修改前
function updateAgentScopeBadge(sid) {
    const badge = document.getElementById('agent-scope-badge');
    const sshStatus = document.getElementById('agent-ssh-status');
    const dbStatus = document.getElementById('agent-db-status');
    if (!badge || !sshStatus || !dbStatus) return;

// 修改后
function updateAgentScopeBadge(sid) {
    const badge = document.getElementById('agent-scope-badge');
    const sshStatus = document.getElementById('agent-ssh-status');
    const dbStatus = document.getElementById('agent-db-status');
    if (!badge || !sshStatus || !dbStatus) return;
    updateAgentConnectionStatus();   // v4.2.1 legacy 会话连接文案随页签切换刷新
```

**(9) `sessionDbType(sid)` 兜底改用会话字段**（约 line 466-468，函数末尾）：

```js
// 修改前
    const c = agentDBConnections.find(x => x.id === agentCurrentDBConn);
    return c ? c.db_type : '';
}

// 修改后（s 已在函数开头定义为 const s = agentSessions.find(x => x.id === sid)）
    const c = agentDBConnections.find(x => x.id === (s && s.db_connection_id));
    return c ? c.db_type : '';
}
```

**(10) `agentApprovalTarget(sid)` legacy 回退改用会话字段**（约 line 1726-1735）：

```js
// 修改前
    const parts = [];
    if (agentCurrentSSHConn) {
        const c = agentSSHConnections.find(x => x.id === agentCurrentSSHConn);
        if (c) parts.push(`SSH: ${c.name}`);
    }
    if (agentCurrentDBConn) {
        const c = agentDBConnections.find(x => x.id === agentCurrentDBConn);
        if (c) parts.push(`DB: ${c.name}`);
    }
    return parts.length ? `🎯 ${parts.join(' / ')}` : '';

// 修改后（函数开头已有 const sc = ...，在其后取 s）
    const s = agentSessions.find(x => x.id === sid);
    const parts = [];
    if (s && s.ssh_connection_id) {
        const c = agentSSHConnections.find(x => x.id === s.ssh_connection_id);
        if (c) parts.push(`SSH: ${c.name}`);
    }
    if (s && s.db_connection_id) {
        const c = agentDBConnections.find(x => x.id === s.db_connection_id);
        if (c) parts.push(`DB: ${c.name}`);
    }
    return parts.length ? `🎯 ${parts.join(' / ')}` : '';
```

**(11) `newAgentSession()` 强制要求范围勾选**（约 line 960-968）：

```js
// 修改前
async function newAgentSession() {
    // v4.0：范围面板有勾选则建「范围会话」（多节点批量）；否则回退 legacy 单连接
    const body = { title: '新会话' };
    if (agentScopeTargets.length > 0) {
        body.scope = agentScopeTargets;
    } else {
        body.ssh_connection_id = agentCurrentSSHConn;
        body.db_connection_id = agentCurrentDBConn;
    }

// 修改后（v4.2.1 操作范围唯一入口 = 左侧面板；无勾选不再静默回退单连接）
async function newAgentSession() {
    if (agentScopeTargets.length === 0) {
        showToast('请先在左侧「操作范围」面板勾选节点，再创建新会话', 'warning');
        return;
    }
    const body = { title: '新会话', scope: agentScopeTargets };
```

> 注：后端 `create_session` / `run_agent` 的 legacy 单连接逻辑**保留不动**（历史 legacy 会话仍可继续跑）。

### 4.2 `templates/index.html`

侧栏提示文案更新（搜 `agent-sidebar-hint`）：

```html
<!-- 修改前 -->
<div class="agent-sidebar-hint">
    💡 批量操作先在范围面板勾选节点，再开新会话。
    连接配置 / 会话历史 / 知识沉淀在右上角「🕘 历史」「⚙️ 管理」。
</div>

<!-- 修改后 -->
<div class="agent-sidebar-hint">
    💡 先在范围面板勾选节点，再开新会话（操作范围唯一入口）。
    连接配置 / 会话历史 / 知识沉淀在右上角「🕘 历史」「⚙️ 管理」。
</div>
```

### 4.3 `static/css/style.css`

连接卡片不再可点（追加到 `.connection-item` 相关规则附近，约 line 5806 后）：

```css
/* v4.2.1 管理抽屉连接卡片纯展示不可点（范围只在左侧面板勾选） */
#agent-ssh-conn-list .connection-item,
#agent-db-conn-list .connection-item {
    cursor: default;
}
```

---

## 收尾

### 5.1 版本号 `config.py`

```python
# 修改前
APP_VERSION = '4.1.0'

# 修改后（注意：version_update.md 已有 v4.2.0 记录但 config 未同步，本次直接对齐到 4.2.1）
APP_VERSION = '4.2.1'
```

### 5.2 `version_update.md` 顶部追加（插在 `## v4.2.0` 之前）

```markdown
## v4.2.1（2026-08-18）

### Agent 范围交互与状态刷新修复

**操作范围栏加宽**：`.agent-sidebar` 260->320px（历史抽屉同宽），节点名/host 悬浮 title 提示，长节点名不再无法辨认。

**会话级历史记忆开关**：`/api/agent/run` 新增 `disable_memory` 参数；会话输入区加「历史记忆」开关（默认开）。关闭后该会话不再召回跨会话长期记忆（环境上下文），完全独立。会话间对话历史本就不共享（每次 run 新建引擎）。

**范围徽标不刷新修复**：管理抽屉补配 SSH/DB 连接保存成功后追加 `refreshAgentScopeResolve()`，徽标立即更新（此前停留在页面加载时的"未配置"）。

**连接卡片去选中**：管理抽屉连接卡片不再可点选（纯管理展示），移除自动选中首个连接与 legacy 单连接会话静默回退；新会话强制要求左侧范围面板先勾选节点（无勾选 toast 提示）。历史 legacy 会话后端逻辑保留可继续执行。
```

### 5.3 验证

```bash
python -m py_compile routes/agent.py agent/engine.py
node --check static/js/agent.js
python -c "from app import create_app; app = create_app(); print('OK')"
```

功能自测点：
1. 左侧范围栏宽度变化，长节点名 hover 出 title；「🕘 历史」抽屉打开仍正好覆盖侧栏
2. 新会话输入区有「历史记忆」勾选框；取消勾选后提问，system prompt 不含"环境上下文（历史记录，供参考）"
3. 范围栏点「配置」→ 添加连接保存 → 徽标立即变 ✅
4. 管理抽屉连接卡片点击无反应（无高亮/选中）；不勾选任何节点点「📝 新会话」→ toast 提示；勾选后新会话页签带范围数字徽标
5. 打开旧的 legacy 会话页签，工具栏仍正确显示其 SSH/DB 连接名
